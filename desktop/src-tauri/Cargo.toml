[package]
name = "veris-desktop"
version = "0.1.0"
edition = "2021"
description = "Veris Desktop — the secure connection layer"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
tauri-plugin-updater = "2"
serde_json = "1"
