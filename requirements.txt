# Veris runtime dependencies.
# The inference layer deliberately uses no vendor SDK — veris/model.py speaks
# HTTP through the standard library, so nothing here pins Veris to one model
# provider and nothing breaks in an air-gapped install.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9   # multipart upload parsing for FastAPI
pypdf>=4.0                # PDF text extraction
python-docx>=1.1          # DOCX text extraction
